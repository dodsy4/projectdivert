.venv/
__pycache__/
.ipynb_checkpoints/
.DS_Store
error.log
*.log

# Runtime-generated uploads (test runs and local dev write here)
static/uploads/
.env
.env.*
!.env.example
*.pem
*.key

# Confidential/client data - never commit real business or client records
encore_clients.csv
clients.csv
sites_clean.csv
sites.xlsx
contact_sheet.csv
contact_sheet
request_sheet.csv
request_sheet

# Mobile app (Expo/React Native)
node_modules/
.expo/
dist/
web-build/

# Local Claude Code tooling. Installed per-developer (e.g. by Neon's onboarding
# command), not part of the application, and the worktrees are scratch copies of
# the repo that must never be committed back into it.
.claude/skills/
.claude/worktrees/
skills-lock.json
